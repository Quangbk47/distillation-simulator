# CI Requirements

PR: formatting/lint, type check, unit/integration/reference tests, production build, secret scan. main requires reviewer + checks. Science model/data change needs scientific lead review + validation regression.
