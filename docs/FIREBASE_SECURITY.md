# Firebase Security Final

The production architecture requires a backend/API. Firebase, if selected, is for authenticated storage/hosting—not for exposing calculation authority. Client may read public demo cases. Server/service account writes reviewed thermo data and validation records. Deny direct client writes to component constants and validation evidence. Secrets/service credentials remain backend-only. Test rules and API authorization in CI/staging.
