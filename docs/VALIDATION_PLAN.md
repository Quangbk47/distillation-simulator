# Validation Plan Final

Primary source may be literature/dataset because experimental data is not currently required. Every case records full citation, table/figure/page, original units, mapped inputs, mapping assumptions and observed outputs.

Report absolute error, relative error and MAE for xD/xB and other available values. The system may calculate metrics now, but PASS/FAIL remains `PENDING_THRESHOLD` until the expert/GVHD supplies: (1) primary metric and (2) allowed percent threshold selected in question 10A. Never label a dataset validated before that value exists.
